spring:
  application:
    name: users
  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: test
    password: test
  sql:
    init:
      mode: embedded
  web:
   error:
    include-binding-errors: always
    include-message: always

logging:
  level:
    io:
      r2dbc:
        h2: DEBUG